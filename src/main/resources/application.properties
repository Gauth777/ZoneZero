# Data source configuration for MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/zonezero
spring.datasource.username=root
spring.datasource.password=Btech@sp1rant 1
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate settings: create tables automatically based on entities
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server port (default is 8080)
server.port=8080

# CORS configuration can be further fine-tuned via code if needed